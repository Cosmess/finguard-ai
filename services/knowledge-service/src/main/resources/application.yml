spring:
  application:
    name: knowledge-service
  datasource:
    url: jdbc:postgresql://localhost:5432/finguard_knowledge
    username: finguard
    password: finguard
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

server:
  port: 8087

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
