spring:
  application:
    name: dispute-agent-service

server:
  port: 8085

dispute:
  agent:
    dispute-service-url: ${DISPUTE_SERVICE_URL:http://localhost:8084}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
